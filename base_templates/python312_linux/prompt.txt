Generate a python app that does the following: Automatically navigates a provided web page based on an initial URL; Remains within that domain; Uses an LLM to determine which HTML element to interact with next on the page from all interactable elements; Based on the type of the specified element, interact appropriately - if it is an input element, use an LLM to determine the appropriate value to select; Record the steps taken so they can be reproduced later, both as Selenium script and UFT VB Script steps; Includes logging and error handling; Continues execution until it encounters an ASP.NET unhandled exception